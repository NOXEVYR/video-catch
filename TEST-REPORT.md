# 0.4.2 落日光轨主题

2026-09-25。应用「落日光轨」图标，并统一窗口品牌图、EXE 与浏览器扩展；深紫灰底色、暖杏色主操作、紫色辅助图形，增加圆角卡片和主题空状态插画。

- 真实窗口截图检查：1440×1000 空状态/合成任务、1280×900 最小窗口，无控件遮挡。
- 26 项 Python 回归测试通过、6 项扩展测试通过。首次多窗口回归暴露关闭后的控制器引用循环可能在 HTTP 线程回收 Tk 资源；已在主线程关闭时断开引用，并完整复测通过。
- 本地最终 ZIP 已验证 CRC、2,230 个包内文件摘要，以及解压后的 EXE 下载字节一致、1～3.25 秒裁剪的音画解码和 2.25 秒时长；打包客户端通过真实鉴权 HTTP 调用。
- GitHub 发布包由对应源码提交重新构建，具体摘要和验证结果以发布附件为准；与本地构建不承诺字节相同。
- 本轮未逐站复测真实视频网站；以下为历史记录。

# 0.4.1 界面与图标更新

2026-09-25，Windows x64。使用现有 Tk/ttk 框架，未增加运行时依赖。

- 新 SVG 标识同步到 PNG、多尺寸 ICO、窗口品牌图、EXE 资源和扩展 16/32/48/128 图标；扩展版本更新为 0.4.1，协议保持版本 3。
- 重新分组链接添加、浏览器来源、视频列表、保存设置；增加空状态、任务计数、完成/进行/失败状态色和按需显示的滚动条；支持原有键盘选择和多选。
- 实际运行截图检查：1440×1000 的空列表/演示任务窗口、1280×900 最小窗口。示例标题与地址为合成数据，不包含用户浏览器信息。
- 26 项 Python 回归测试通过，6 项 Node 扩展测试通过。首次回归的一次来源拒绝请求出现 Windows 本机连接中止；完整复测通过。界面 idle 布局回调在关闭时取消。
- 此轮未测试真实视频网站。打包与本机安装结果另见本轮交付记录，历史记录仅用于追溯。

# 0.4.0-ai.1 本地构建验收

2026-09-24，Windows x64，Python 3.11.9。源码基于 NOXEVYR/video-catch 的 a2fe15f 快照，原版 ZIP 的 SHA-256 为 `796eb2831f2871b129a3cc708c352323fdc7d5a10def553feb6bec0313cc5024`，已校验完整下载、ZIP CRC 和全部包内文件清单。

- 后端使用仓库规定命令 `.build/venv/Scripts/python.exe -m unittest discover -s tests -v`，26 项测试通过；Node 命令 `node --test tests/extension.test.cjs`，原有 6 项通过。AI 测试走真实本机 HTTP 与 Tk 主线程队列，覆盖未启用、无效配对、网页/扩展来源拒绝、参数验证、取消排队、过期请求不执行和禁止覆盖已有裁剪文件。
- 合成媒体链路：监听测试标签页 → 捕获本地 HTTP 视频 → AI 提交下载 → SHA-256 一致 → AI 提交 1～3.25 秒裁剪 → 片段音画解码与 2.25 秒时长检查 → 源文件摘要不变。
- 从 Windows ZIP 解压后的 `VideoCatch.exe` 运行 `--verify-download` 与 `--verify-clip-json`，验证冻结进程、下载及裁剪子进程均能产出有效视频。`VideoCatchAI.exe` 成功通过真实鉴权 HTTP 调用运行中的源码应用。详细结果见 `PACKAGE-VERIFICATION.json`。
- 扩展协议保持版本 3；浏览器扩展代码未改动，随新版分发的扩展仍为 0.3.0。
- 未提交、推送或发布。未在本轮操作用户浏览器账户或对第三方视频网站逐站验收；未完成人工可见界面验收。旧版记录仅为历史，不代表本次复测。

## 0.3.0 验收记录（历史）

2026-09-11，Windows x64。

- 21 项 Python 测试、6 项 Node 测试通过：包括原文件字节一致、HLS 与 B站分离音视频合并、取消重试、鉴权和标签页隔离；新增 YouTube 单视频归一、分片去重、重复开始/停止监听以及网络参数检查。
- YouTube 示例 `F8zqVO8UqTM`：通过本机已有代理，配合内置版本的 Deno 2.9.6、yt-dlp 2026.8.19、yt-dlp-ejs 0.8.0 成功下载。文件 85,677,064 字节，1920×1080 VP9、双声道 Opus，时长 264.99 秒；未转码，输出 WebM。完整音视频解码检查通过。
- 监听入口拆分为「开始监听」「停止监听」，增加随连接与发现状态变化的步骤提示。浏览器扩展独立打包并附固定目录安装、更新与重新配对说明。
- 自带运行组件的 Windows EXE 使用相同下载队列完成该 YouTube 示例下载，完整音视频解码退出码 0。高 DPI 界面经过截图检查，下载连接与监听按钮可见。
- 样本媒体、代理配置、捕获地址和浏览器测试数据不进入发布包；下载测试成功不代表所有地区、账号和站点均可用。

## 0.2.0 B 站修复验收

2026-09-11，Windows x64、独立测试配置的 Edge 152，未导入用户浏览器账号。

- 用户提供的视频 `BV1WuYh6VEaS`：真实浏览器成功发现 6 个视频格式和 3 个音频格式，桌面桥接条目升级为「B站音画合并」。
- 从浏览器捕获的媒体信息直接下载并合并，输出 22,771,267 字节 MP4，时长 8 分 58.52 秒，852×480 AV1 视频、双声道 AAC 音频。FFmpeg 对完整音视频解码成功，退出码 0。这是测试会话可用的清晰度，不承诺登录账号或所有清晰度可用。
- 18 项 Python 测试通过，新增 B 站勾选即发现、追踪参数去重、分 P 区分、过期页面回调拒绝、元数据升级、音视频轨道校验以及分离 M4S 下载合并测试。
- 6 项 Node 测试通过，包含播放器清除初始数据后的捕获、不同视频/分 P 隔离、fetch 原响应保持和复用 XHR 隔离。
- 其他站点新增网页播放器识别与网页解析入口；没有逐站验收，不能据此认定所有网站兼容。
- 0.1.0 原程序包保留；新版位于版本独立目录，校验信息见 `releases/checksums-v0.2.0.json`。媒体样本、签名 URL、临时浏览器状态不进入程序包。

以下为旧版历史记录。

## 0.1.0 本地验收记录

测试日期：2026-09-10。环境：Windows x64、Python 3.12.14；浏览器为独立临时配置的 Edge 152。使用本机生成的媒体样本，不使用用户账号或现有浏览器配置。

- 12 项 Python 测试通过：标签页选择隔离、暂停、去重、关闭移除、URL 验证、普通请求头筛选、本机桥接鉴权与来源检查、错误请求、原文件下载、HLS 合并、失败处理、取消和重试。
- 2 项 Node 测试通过：识别媒体清单、过滤零散片段；扩展脚本语法检查通过。
- 真实 Edge 加载解压扩展并通过弹窗配对，能同步标签页；已选择的测试页播放后捕获 1 条 MP4，未选择的测试页播放后候选数仍为 1。
- 自带运行组件 EXE 的下载队列成功保存本机 MP4，SHA-256 与源文件相同。
- 自带运行组件 EXE 成功下载 HLS 并合并；FFmpeg 验证音轨和视频轨道可完整解码。
- 取消正在传输的任务后，后台下载进程退出；重试同一任务成功，产物与源文件字节相同。
- 桌面界面经过截图检查，修复高 DPI 下窗口超出屏幕导致底部控件不可见的问题。
- 程序 ZIP 校验为单一 `VideoCatch/` 根目录，ZIP CRC 检查通过，大小与 SHA-256 见 `releases/checksums.json`。

范围：未逐站验证互联网视频站点；Chrome 扩展使用相同 Chromium API，但本次真实浏览器验收只在 Edge 完成。DASH 已实现清单识别并交由 yt-dlp 处理，本轮未作独立 DASH 端到端媒体测试。DRM、无法导出 URL 的专有桌面客户端、登录凭证需求及直播收尾不在本版保证范围内。
