# 拾影 VideoCatch 0.5.0 · Windows / macOS 录屏与 AI 控制

- Windows 10 / 11 x64 与 macOS 14+ 提供网页视频下载、本地裁剪、屏幕 / 区域 / 窗口 / 摄像头录制、摄像头画中画、暂停继续和 PNG 截图；平台及架构以 [0.5.0 发布页](https://github.com/NOXEVYR/video-catch/releases/tag/v0.5.0) 实际附件为准。
- Windows 全屏覆盖虚拟桌面并支持系统声音回环；Mac 全屏仅录主显示器，区域和窗口须完整位于同一显示器。Mac 窗口模式裁剪屏幕矩形，遮挡会入镜，目标移动或调整大小后会停止并保存。
- Mac 系统声音需要已有并配置好路由的 BlackHole、Loopback 等虚拟回环输入，并手动选择；程序不会安装音频驱动或改变系统声音输出。
- 浮动工具条提供暂停、继续及停止保存，支持画质 / 帧率、倒计时、定时停止、画笔及可选快捷键。桌面笔迹仅进入全屏和区域录制 / 截图。停止后须等「已保存」才取得完整 MP4。
- AI 接口增加 `capture_sources`、`record_start`、`record_pause`、`record_resume`、`record_stop`、`record_state` 和异步 `screenshot`；Windows 复制 PowerShell 指引，Mac 复制 zsh/bash 指引，均不含配对密钥。
- Mac 需保留整个 `VideoCatch` 目录，`VideoCatch.app` 与 `VideoCatchAI`、文档及扩展放在同一目录。Mac 配对设置保存在 `~/Library/Application Support/VideoCatch/ai-client.json`。
- Mac 包未进行 Developer ID 签名或 Apple 公证。屏幕、摄像头、麦克风及系统权限交互尚未经过人工硬件验收；自动构建或合成媒体验证不代表这些功能已实机验收。具体构建和验证结果见 [验证记录](TEST-REPORT.md)。
- 浏览器扩展沿用 0.4.2 和桥接协议 3。既有公开包及以下本地开发历史继续保留。

# 拾影 VideoCatch 0.5.0-dev.1 · 本地录屏与截图（历史本地开发版，未公开发布）

- 新增「录屏 / 截图」面板：全屏、多显示器区域框选、窗口客户区和仅摄像头录制，屏幕画面可叠加右下角摄像头。
- 支持无声、系统声音、麦克风及混合音源；提供帧率、画质预设、定时停止、倒计时、鼠标指针与录制时最小化主窗口选项。
- 浮动工具条支持暂停、继续和停止保存；录制输出 MP4，全屏 / 区域 / 窗口截图输出 PNG。停止后需等待「已保存」，不能把「正在保存」当作完成。
- 新增桌面画笔、颜色与粗细、撤销、清空及结束绘制。桌面笔迹仅进入全屏或区域录制 / 截图，不进入独立窗口或摄像头画面。
- 可选全局快捷键 Ctrl + Alt + F9 / F10 / F11 / F12，分别用于录制停止、暂停继续、截图及画笔；注册冲突会显示提示。
- AI 接口新增采集来源枚举、录制启停与暂停继续、当前录制状态和异步截图。录制结束与截图完成仍须轮询任务状态并读取最终文件路径。
- 本节仅描述开发中的源码功能；没有新增公共下载或替换既有发布包。0.4.3 本地更新及 0.4.2 公开版本历史保留，浏览器扩展继续沿用 0.4.2 和桥接协议 3。

# 拾影 VideoCatch 0.4.3 · 一键 AI 协作

- 顶部新增「开启协作并复制」：自动本机配对、开启接口并复制带真实客户端路径的指引，指引不包含密钥。
- 客户端区分未配对、配对过期、接口关闭、连接失败及参数错误；AI 可读取参数说明、默认目录及裁剪时间信息。
- 浏览器扩展继续使用 0.4.2 和桥接协议 3；已有扩展无需替换。本次仅本地交付，尚未公开发布。

以下为历史记录。

# 拾影 VideoCatch 0.4.2 · 落日光轨与 AI 控制

- 新增本机 AI 控制接口：标签页监听、导入与下载、任务状态、取消和本地视频裁剪。默认关闭，启用后通过每次启动更新的配对码验证。
- 本地视频按起止时间精确裁剪为 H.264/AAC MP4，另存新文件，保留原视频。
- 采用「落日光轨」图标，统一深紫灰与暖杏色界面、圆角分区、功能图形和空状态指引。Windows 程序与浏览器扩展图标同步更新。
- 修复关闭窗口后的 Tk 资源引用循环。26 项 Python 和 6 项扩展测试通过；发布流程另验最终 ZIP、文件摘要、冻结 EXE 下载与裁剪、客户端 HTTP 调用。

完整解压 Windows 包后运行 VideoCatch.exe，保留全部文件。AI 命令行客户端为 VideoCatchAI.exe。浏览器扩展可单独下载，解压到固定目录后加载；更新已有扩展后点击“重新加载”，检查版本为 0.4.2，再重新配对。

仅验证合成媒体及本机接口，不代表所有第三方视频网站可用；不提供登录、DRM 或网站访问限制绕过。历史版本保留。附件校验信息见 checksums-v0.4.2.json 和 extension-checksums-v0.4.2.json。
