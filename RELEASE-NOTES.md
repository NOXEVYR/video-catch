# 拾影 VideoCatch 0.4.2 · 落日光轨与 AI 控制

- 新增本机 AI 控制接口：标签页监听、导入与下载、任务状态、取消和本地视频裁剪。默认关闭，启用后通过每次启动更新的配对码验证。
- 本地视频按起止时间精确裁剪为 H.264/AAC MP4，另存新文件，保留原视频。
- 采用「落日光轨」图标，统一深紫灰与暖杏色界面、圆角分区、功能图形和空状态指引。Windows 程序与浏览器扩展图标同步更新。
- 修复关闭窗口后的 Tk 资源引用循环。26 项 Python 和 6 项扩展测试通过；发布流程另验最终 ZIP、文件摘要、冻结 EXE 下载与裁剪、客户端 HTTP 调用。

完整解压 Windows 包后运行 VideoCatch.exe，保留全部文件。AI 命令行客户端为 VideoCatchAI.exe。浏览器扩展可单独下载，解压到固定目录后加载；更新已有扩展后点击“重新加载”，检查版本为 0.4.2，再重新配对。

仅验证合成媒体及本机接口，不代表所有第三方视频网站可用；不提供登录、DRM 或网站访问限制绕过。历史版本保留。附件校验信息见 checksums-v0.4.2.json 和 extension-checksums-v0.4.2.json。
